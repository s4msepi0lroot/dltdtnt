<Window x:Class="DeltaDotNet.Client.Views.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeltaDotNet" SizeToContent="Height" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Border Style="{StaticResource DeltaBox}">
        <StackPanel>
            <TextBlock x:Name="PromptText" Margin="0,0,0,8" />
            <TextBox x:Name="InputBox" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="OK" Width="90" IsDefault="True" Click="Ok_Click" />
                <Button Content="Cancel" Width="90" Margin="8,4,0,4" IsCancel="True" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
