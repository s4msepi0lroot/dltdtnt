<UserControl x:Class="DeltaDotNet.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Border Style="{StaticResource DeltaBox}" MaxWidth="520" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Style="{StaticResource TitleText}" Text="* Sign in" />
                <TextBlock Style="{StaticResource SubtleText}"
                           Text="Your account lives on the DeltaDotNet server. Everything is relayed through it — no P2P." />

                <TextBlock Margin="0,12,0,0" Text="Server URL" />
                <TextBox x:Name="ServerBox" />

                <TextBlock Margin="0,8,0,0" Text="Username" />
                <TextBox x:Name="UsernameBox" />

                <TextBlock Margin="0,8,0,0" Text="Password" />
                <PasswordBox x:Name="PasswordBox" />

                <CheckBox x:Name="RememberBox" Content="Stay signed in on this PC" IsChecked="True" />

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button x:Name="LoginButton" Content="LOG IN" Width="140" Click="Login_Click" IsDefault="True" />
                    <Button x:Name="RegisterButton" Content="REGISTER" Width="140" Margin="10,4,0,4" Click="Register_Click" />
                </StackPanel>

                <TextBlock x:Name="MessageText" Margin="0,10,0,0" Foreground="{DynamicResource DangerBrush}" Text="" />
            </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>
