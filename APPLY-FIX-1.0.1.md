# Rift Spotify 1.0.1 — applying the crash fix

The previous download did not update an existing GitHub Desktop checkout automatically. This bundle contains the actual changed source files and a visible patch.

Replace the project files with this bundle, or apply `rift-spotify-1.0.1-event-bus.patch` from the repository root. Then rebuild and replace the old `rift-spotify-1.0.0.jar` with the new 1.0.1 JAR.
